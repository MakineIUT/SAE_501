// Configuration globale de l'application

// Mettre à false pour utiliser la vraie API (quand elle sera prête)
// Mettre à true pour utiliser le mode test (localStorage local sans backend)
export const USE_MOCK_AUTH = false
